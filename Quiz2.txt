CREATE TABLE PROJECTS(
ID NUMBER (10) PRIMARY KEY,
CODE VARCHAR2(255) NOT NULL,
MANAGER_ID NUMBER(10) NOT NULL,
BUDGET NUMBER(10,2) NOT NULL,
DATE_OF_BEGINNING DATE,
DATE_OF_ENDING DATE
)



CREATE TABLE EMPLOYEES(
ID NUMBER (10) PRIMARY KEY,
MANAGER_ID NUMBER (10)
CONSTRAINT MANAGERS_ID1 REFERENCES PROJECTS (ID),
DATE_OF_JOINING DATE NOT NULL,
FULL_NAME VARCHAR2(255) NOT NULL
)

CREATE TABLE SALARIES (
ID NUMBER(10) PRIMARY KEY,
EMPLOYEE_ID NUMBER(10) NOT NULL
CONSTRAINT EMPLO REFERENCES EMPLOYEES (ID),
PROJECT_ID NUMBER(10)
CONSTRAINT PROJ REFERENCES PROJECTS (ID),
SALARY NUMBER (10,2)
)

ALTER TABLE EMPLOYEES 
ADD CONSTRAINT TEST_1 FOREIGN KEY (MANAGER_ID) REFERENCES EMPLOYEES (ID);